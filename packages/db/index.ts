/**
 * Public surface of @undoverse/db.
 * Re-export the generated Prisma client + all model types so consumers do a
 * single `import { PrismaClient, Project } from "@undoverse/db"`.
 */
export * from "@prisma/client";
export { PrismaClient } from "@prisma/client";
